# Upstream Omniroute endpoint
OMNIROUTE_URL=http://localhost:8000
OMNIROUTE_API_KEY=

# Proxy server configuration
PORT=3005
HOST=0.0.0.0

# Optional custom path for storage (defaults to ./data)
# DATA_DIR=./data
